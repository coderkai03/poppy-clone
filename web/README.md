# web

Next.js frontend and API routes for Poppy Clone.

See the [root README](../README.md) for setup, environment variables and how this
half talks to the `engine/` half.

```bash
npm install
npm run dev        # http://localhost:3000
npm run typecheck
npm run lint
```
